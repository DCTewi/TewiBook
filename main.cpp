#include "main.h"

int main()
{
    MenuController menu;
/*
    while (1)
    {
        int opt = getkey();

        std::cout << opt << "\n";
        if (opt == 3) return 0;
    }
*/
    return menu.show();
}
